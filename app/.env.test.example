# Points at the mindful-me-test Supabase project (used for the develop
# branch and local testing — never production data). Copy to .env to work
# against it locally: cp app/.env.test.example app/.env
# The anon/publishable key is safe to expose in a client bundle by design —
# it identifies the project, not a secret; every table it can reach is
# still gated by row-level security.
VITE_SUPABASE_URL=https://iawbubbywkcxwvzyjaar.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_NlwE0x7nd4gKg6piT6fKzg_8V7LqMl8
